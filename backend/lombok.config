# 批次9（R4-1.55）：让 Lombok 生成的 accessor/构造器/builder 方法携带 @lombok.Generated 注解。
# SpotBugs 通过 backend/spotbugs-exclude.xml 规则1精确豁免这些"生成代码"上的 EI_EXPOSE_REP/EI_EXPOSE_REP2，
# 而不是整类豁免——手写业务代码上的 EI 告警仍会被 enforce 门禁拦截。
lombok.addLombokGeneratedAnnotation = true
