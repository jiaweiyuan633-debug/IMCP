# 双端管理脚手架：分批开发计划与提示词（待审核）

> 依据：《前后端双端管理脚手架项目开发计划书（细化版）v2.0》
> 状态：执行中（B1-B6 已完成，待确认后进入 B7）
> 批次：B1-B10，共 10 批
> 执行原则：每批完成并验证后暂停汇报，经确认后再进入下一批；不越批、不欠验证。

## 1. 环境现状与准备

已确认可用的环境：

| 工具 | 本机版本 | 用途 |
| --- | --- | --- |
| Java | 21.0.11 LTS | Spring Boot 3 后端 |
| Maven | 3.9.9 | 后端构建 |
| Node.js | 24.18.0 | 前端运行 |
| npm / pnpm | 11.16.0 | 前端依赖管理 |
| uv | 0.11.31 | Python 环境管理 |

需要补齐或确认的环境：

| 项 | 现状 | 计划处理方式 |
| --- | --- | --- |
| Python | 未安装 | 用 uv 托管 Python 3.11+，不依赖系统 Python |
| MySQL | MySQL80 服务已安装但停止 | 执行 B1/B2 时启动服务并创建 `admin_scaffold` 数据库 |
| Redis | 未安装 | 执行 B2 前安装并启动 Redis（本机服务或便携版） |
| Docker | 未安装 | 交付 Docker Compose 配置；若执行 B10 前仍无 Docker，则做 YAML 校验并给出全新环境部署说明 |
| Git 仓库 | 当前目录还不是仓库 | B1 初始化 `main` / `dev`，后续在 `feature/` 分支上开发并合并到 `dev` |

版本偏差说明：项目书前端基线为 Vite 5.x，但本机是 Node 24，为避免兼容问题，前端采用 Vite 7.x，其余技术选型按项目书执行。

## 2. 需要你确认的决策

1. 当前目录 `C:\Users\Lenovo\Desktop\双端管理系统` 直接作为 monorepo 根目录，不再嵌套一层 `admin-scaffold`。
2. 采用项目书 0.1 节全部默认建议：前端 TypeScript、FastAPI 异步任务 + Redis 状态、Flyway 管理 DDL、交付 Docker Compose、一期示例能力为文本摘要和关键词抽取。
3. 允许执行时启动 MySQL80、安装并启动 Redis；如需联网下载，允许临时网络审批。
4. 每批完成后我暂停并汇报验证结果，你确认后再执行下一批。

## 3. 批次总览

| 批次 | 名称 | 一句话验收 | 依赖 |
| --- | --- | --- | --- |
| B1 | 项目准备与三端脚手架 | 三端项目可启动，Flyway 初始 DDL 可执行 | 无 |
| B2 | Java 基础能力与认证 | Postman 可登录、刷新、退出并访问受保护接口 | B1 |
| B3 | Java 系统管理模块 | 用户/角色/菜单 CRUD、授权、日志、在线用户可用 | B2 |
| B4 | Python AI 服务 | FastAPI 任务接口、Redis 状态、示例算法、回调可用 | B1 |
| B5 | Java AI 编排与双服务联调 | Java 创建 AI 任务到 Python 执行并回调落库全链路跑通 | B3、B4 |
| B6 | 前端基础与登录 | 登录、动态路由、布局、主题、刷新不丢权限 | B2 |
| B7 | 前端系统管理页面 | 用户/角色/菜单/日志/在线用户页面可操作 | B6 |
| B8 | 前端 AI 与看板 | 看板有真实数据，AI 任务创建、轮询、结果展示可跑通 | B7、B5 |
| B9 | 联调与验收修复 | 全流程手工验收通过，缺陷清零，错误场景有提示 | B8 |
| B10 | 文档与交付 | README、接口/数据库/部署文档、Docker Compose 齐全 | B9 |

## 4. 分批执行计划与提示词

每个批次提示词可直接作为下一步执行指令。执行时严格按“实现 -> 自测 -> 验证 -> 汇报”的顺序完成。

### B1：项目准备与三端脚手架

**目标**

在 `C:\Users\Lenovo\Desktop\双端管理系统` 初始化 monorepo：`frontend`、`backend`、`ai-service`、`docs`、`docker` 五个目录；三端项目均可启动；Flyway 初始 DDL 包含 10 张核心表并写入种子数据。

**主要产出**

- Git 仓库：`main`、`dev` 分支，`.gitignore`，README 启动说明。
- 后端：Spring Boot 3.3.x + Java 21，Maven 依赖含 Web、Validation、Security、MyBatis-Plus、MySQL、Flyway、Redis、jjwt、Lombok、Hutool、Knife4j；`application.yml`、`application-dev.yml`、`.env.example`。
- Flyway：`V1__init.sql` 建 10 张表并插入初始菜单、角色、管理员账号。
- 前端：Vite 7 + Vue 3 + TypeScript + Ant Design Vue 4 + Pinia + Vue Router + Axios + ECharts + ESLint/Prettier；目录按项目书第 4 章预留。
- AI 服务：uv 管理 Python 3.11+，FastAPI + uvicorn + Pydantic + httpx + redis-py + pytest + ruff；`/health`、`/api/v1/ping` 可访问。

**验收标准**

- `mvn -q -DskipTests package` 通过。
- `pnpm build`（含 `vue-tsc`）通过。
- `uv run pytest` 通过；AI 服务启动后 `/health` 返回 200。
- MySQL 启动后 Flyway 迁移成功，10 张表存在，管理员账号可查到。

**B1 提示词**

```text
请完成 B1：项目准备与三端脚手架。
依据项目书 v2.0 第 4、5、15 章，在当前目录创建 monorepo：
frontend、backend、ai-service、docs、docker。初始化 Git 仓库并建立 main/dev 分支。
后端使用 Spring Boot 3.3.x + Java 21 + Maven，按项目书锁定依赖；
前端使用 Vite 7 + Vue 3 + TypeScript + Ant Design Vue 4 + Pinia + Vue Router + Axios + ECharts，目录预留到项目书结构；
AI 服务用 uv 初始化 Python 3.11+，实现 FastAPI 健康检查和连通性接口。
编写 Flyway V1 初始 DDL：sys_user、sys_role、sys_menu、sys_user_role、sys_role_menu、
sys_login_log、sys_oper_log、ai_service_config、ai_task、ai_task_result，
并插入根菜单、admin 角色、admin 管理员（BCrypt 密码）、示例菜单和 AI 服务配置。
验证：mvn 打包通过、pnpm build 通过、uv run pytest 通过、AI 服务 /health 返回 200、
MySQL 执行 Flyway 后表结构和种子数据正确。
完成后汇报每项验证结果和启动命令，不要进入 B2。
```

### B2：Java 基础能力与认证

**目标**

完成 Java 后端 `common` 与 `security` 两套基础能力，实现登录、刷新、退出、当前用户、修改密码，接入 Redis 的 Token 管理和在线用户。

**主要产出**

- 统一响应 `Result<T>`、全局异常、requestId、业务错误码。
- JWT 生成/校验、认证过滤器、`@PreAuthorize` 权限入口。
- `/api/auth/login`、`/api/auth/refresh`、`/api/auth/logout`、`/api/auth/me`、`/api/auth/password`。
- Redis：`login:token:{jti}`、黑名单、在线用户。
- 登录日志记录、CORS、Knife4j 文档入口。

**验收标准**

- 单元测试：JWT、异常、密码加密通过。
- 集成测试：登录成功返回 accessToken/refreshToken；未带 Token 访问受保护接口返回 401；刷新、退出、修改密码可用。
- 登录成功后 Redis 有 Token 记录，退出后失效。
- Knife4j 页面可访问。

**B2 提示词**

```text
请完成 B2：Java 基础能力与认证。
依据项目书第 6.2、8、10.1、10.2、10.4 章，实现 common 与 security：
Result<T> 统一响应、GlobalExceptionHandler、requestId、业务错误码；
JWT 工具、Spring Security 认证过滤器、登录/刷新/退出/me/修改密码接口；
Token 写入 Redis 并支持黑名单与在线用户；
登录日志记录、跨域配置、Knife4j。
验证：mvn test 通过；启动 MySQL 和 Redis 后，用 curl/Postman 跑通
登录 -> 访问受保护接口 -> 刷新 -> 退出，并确认 401/403 行为正确；
确认 Redis 中 Token 记录存在且退出后失效；Knife4j 页面可访问。
完成后汇报验证结果和接口示例，不要进入 B3。
```

### B3：Java 系统管理模块

**目标**

实现用户、角色、菜单管理，角色授权，权限校验，登录/操作日志，在线用户与缓存管理，以及看板统计接口。

**主要产出**

- 用户/角色/菜单 CRUD、分页、菜单树、用户状态、角色授权。
- `@PreAuthorize` 按钮级权限（如 `system:user:add`）。
- `/api/monitor/login-log`、`/api/monitor/oper-log`、`/api/monitor/online`、`/api/monitor/cache/{key}`。
- 操作日志 AOP 异步落库。
- `/api/monitor/stats`：用户/角色/菜单数量、日志数量、AI 任务状态统计，供前端看板使用。

**验收标准**

- 管理员可完成用户、角色、菜单的增删改查与授权。
- 无权限角色访问受限接口返回 403。
- 登录和关键操作自动写入日志；在线用户可查询并踢出。
- 看板统计接口返回真实数字。

**B3 提示词**

```text
请完成 B3：Java 系统管理模块。
依据项目书第 6.3、8、10.2、10.4 章，实现 system 与 monitor：
用户/角色/菜单 CRUD、分页查询、菜单树、用户状态切换、角色授权菜单；
按钮级权限注解校验；登录日志、操作日志 AOP 异步记录；
在线用户查询与强制下线；缓存 Key 删除；
新增 /api/monitor/stats 看板统计接口。
验证：mvn test 通过；用 curl/Postman 以 admin 登录后完成
用户/角色/菜单 CRUD 和授权；创建低权限角色验证 403；
确认日志、在线用户、缓存清理、stats 接口均返回正确数据。
完成后汇报验证结果，不要进入 B4。
```

### B4：Python AI 服务

**目标**

完成 FastAPI AI 服务全部功能：任务创建、查询、重试、Redis 状态同步、异步执行、示例算法、回调 Java。

**主要产出**

- `/health`、`/api/v1/ping`、`/api/v1/tasks`、`/api/v1/tasks/{task_id}`、`/api/v1/tasks/{task_id}/retry`。
- 任务管理器：Redis Key `ai:task:{task_no}`，`QUEUED -> RUNNING -> SUCCEEDED/FAILED`，失败重试。
- 示例算法：`text_summary` 文本摘要、`keyword_extract` 关键词抽取，可扩展注册表。
- 回调 Java：请求头 `X-Ai-Service-Token`，结果写入 Java 回调接口。
- 环境变量：`APP_NAME`、`REDIS_URL`、`CALLBACK_TOKEN`、`DEFAULT_TIMEOUT_SECONDS`。
- pytest 测试：Pydantic 校验、示例算法、任务状态流转。

**验收标准**

- `uv run pytest` 通过。
- 服务启动后 curl 可创建任务、查询状态、触发重试。
- Redis 中任务状态按状态机变化。
- 回调使用正确 Token，可配置目标地址。

**B4 提示词**

```text
请完成 B4：Python AI 服务。
依据项目书第 11 章和第 6.5、6.6、7 章，完成 FastAPI 服务：
/health、/api/v1/ping、/api/v1/tasks、/api/v1/tasks/{task_id}、
/api/v1/tasks/{task_id}/retry；任务状态写入 Redis（ai:task:{task_no}）；
用 asyncio 异步执行；实现 text_summary 和 keyword_extract 两个可扩展示例算法；
任务完成后按 callback_url 回调 Java，请求头携带 X-Ai-Service-Token；
失败按策略重试；环境变量通过 .env.example 声明。
测试：pytest 覆盖 Pydantic 校验、算法输出、状态流转，可使用 fakeredis。
验证：uv run pytest 通过；启动服务后用 curl 完成创建、查询、重试；
确认 Redis 状态变化和回调请求正确。
完成后汇报验证结果，不要进入 B5。
```

### B5：Java AI 编排与双服务联调

**目标**

完成 Java 侧 AI 配置、任务管理、Python 客户端与回调接口，打通 Java -> Python -> Java 全链路。

**主要产出**

- `/api/ai/config`、`/api/ai/config/{id}`。
- `/api/ai/tasks` 创建/分页、`/api/ai/tasks/{id}` 详情与结果、`DELETE /api/ai/tasks/{id}` 取消。
- `/api/ai/callback/task`：Token 校验、`task_no` 幂等、只允许合法状态流转。
- Python 客户端：健康检查、创建任务、超时与错误提示。
- 超时/重试扫描：状态机按项目书第 7 章执行。

**验收标准**

- Java 创建任务后 Python 收到请求，执行完成后回调 Java，任务变为 `SUCCEEDED` 且结果入库。
- 模拟算法失败可看到重试记录，重试耗尽后 `FAILED`。
- 重复回调不重复写入，伪造 Token 回调被拒绝。
- AI 服务不可用时 Java 返回明确错误。

**B5 提示词**

```text
请完成 B5：Java AI 编排与双服务联调。
依据项目书第 6.4、6.6、7、10.2、10.3 章，完成 ai 模块：
AI 服务配置 CRUD；任务创建/分页/详情/取消；Python 客户端支持健康检查、
创建任务、超时与错误处理；回调接口校验 X-Ai-Service-Token、
校验 task_no 并幂等处理，状态按 PENDING/QUEUED/RUNNING/SUCCEEDED/FAILED/CANCELLED 流转；
实现超时扫描和失败重试；AI 服务不可用时返回“AI 服务不可用”明确错误。
验证：同时启动 MySQL、Redis、backend、ai-service，跑通
Java 创建任务 -> Python 执行 -> 回调 Java -> SUCCEEDED 且结果入库；
再验证失败重试、伪造 Token 拒绝、重复回调幂等、AI 服务挂掉时的提示。
完成后汇报完整链路证据，不要进入 B6。
```

### B6：前端基础与登录

**目标**

完成前端工程基础：企业级布局、主题、路由守卫、状态管理、Axios 封装、登录页和个人中心。

**主要产出**

- 左侧菜单 + 顶部区域 + 内容区布局，侧边栏折叠、多标签、面包屑、浅色/暗黑主题。
- Axios 封装：Token 注入、401 自动刷新、统一错误提示、`Result<T>` 泛型。
- Pinia：`user`、`permission`、`app`；动态路由组装；刷新页面重新加载权限。
- 登录页、个人中心、修改密码。

**验收标准**

- `pnpm build` 通过。
- 使用真实 Java 后端登录成功，菜单按角色显示。
- 页面刷新后权限和菜单不丢失。
- Token 过期后自动刷新，刷新失败跳转登录页。

**B6 提示词**

```text
请完成 B6：前端基础与登录。
依据项目书第 8.3、9.1、9.2、9.3 章，完成前端基础工程：
企业级后台布局（左侧菜单、顶部、内容区、折叠、多标签、面包屑、浅色/暗黑主题）；
Axios 封装（Token 注入、401 刷新、统一错误、Result<T>）；
Pinia 的 user/permission/app store；动态路由与路由守卫，刷新后重新加载权限；
登录页、个人中心、修改密码页面。
验证：pnpm build 通过；连接真实 Java 后端完成登录；
确认动态菜单按角色显示、刷新不丢权限、Token 过期自动刷新、
主题切换和布局交互正常。完成后汇报验证结果，不要进入 B7。
```

### B7：前端系统管理页面

**目标**

完成系统管理和监控页面，沉淀可复用 ProTable、ProSearchForm、ModalForm、StatusTag 组件。

**主要产出**

- 用户、角色、菜单页面：查询、分页、新增、编辑、删除、状态切换。
- 角色授权页：菜单树勾选授权。
- 日志、在线用户、缓存管理页面。
- 按钮权限指令 `v-permission`。
- 通用组件至少被 2 个页面复用。

**验收标准**

- `pnpm build` 通过。
- 连接真实后端完成用户/角色/菜单 CRUD 和授权。
- 低权限账号看不到无权限按钮，接口调用返回 403 时有明确提示。
- 日志、在线用户、缓存页面展示真实数据。

**B7 提示词**

```text
请完成 B7：前端系统管理页面。
依据项目书第 6.3、9.2、9.4 章，实现 system 与 monitor 页面：
用户/角色/菜单管理（查询、分页、新增、编辑、删除、状态切换）；
角色授权菜单树；登录日志、操作日志、在线用户、缓存管理；
实现 ProTable、ProSearchForm、ModalForm、StatusTag 通用组件并至少复用两处；
实现 v-permission 按钮权限指令。
验证：pnpm build 通过；连接真实 Java 后端完成全部 CRUD 与授权；
低权限账号验证按钮隐藏和 403 提示；日志、在线用户、缓存页面展示真实数据。
完成后汇报验证结果，不要进入 B8。
```

### B8：前端 AI 与看板

**目标**

完成数据看板和 AI 任务页面，与 B5 服务全链路对接。

**主要产出**

- 首页看板：ECharts 展示用户/角色/菜单数量、任务状态分布、日志趋势，使用 `/api/monitor/stats` 真实数据。
- AI 任务页：创建任务、列表分页、详情页 2 秒轮询、状态标签、结果展示。
- AI 配置页：服务地址、密钥、超时、启停管理。
- 空态、加载态、错误态完整。

**验收标准**

- `pnpm build` 通过。
- 看板数据与后端统计一致。
- 浏览器创建 AI 任务后能观察到状态轮询到 `SUCCEEDED` 并展示结果。
- 任务失败/取消/超时有明确状态和提示。

**B8 提示词**

```text
请完成 B8：前端 AI 与看板。
依据项目书第 9.2、9.4 章和第 6.4 章，实现：
首页数据看板（ECharts 展示用户/角色/菜单、AI 任务状态分布、日志趋势，
数据来自 /api/monitor/stats）；
AI 任务列表、创建任务、任务详情（2 秒轮询，终态停止）、结果展示；
AI 服务配置页面；覆盖空态/加载态/错误态。
验证：pnpm build 通过；连接真实后端，看板数字与接口一致；
浏览器创建文本摘要和关键词抽取任务，轮询到 SUCCEEDED 并展示结果；
失败和取消场景提示正确。完成后汇报验证结果，不要进入 B9。
```

### B9：联调与验收修复

**目标**

按项目书 P4 验收标准做全流程回归，修复缺陷，补足测试。

**主要产出**

- 全流程验收单：登录 -> 动态菜单 -> 系统管理 CRUD -> 角色权限 -> AI 任务 -> 结果展示。
- 异常场景：Token 过期、AI 服务挂掉、参数错误、403。
- 浏览器兼容：Chrome、Edge。
- 补足关键单元/接口测试，修复发现的缺陷。

**验收标准**

- 验收单全部通过，关键缺陷清零。
- 无刷新丢权限、无状态机错乱、无页面报错。
- 自动化冒烟脚本可重复执行。

**B9 提示词**

```text
请完成 B9：联调与验收修复。
依据项目书第 12.6、14 章，执行全流程回归：
登录 -> 动态菜单 -> 用户/角色/菜单 CRUD 与授权 -> 日志/在线用户 ->
AI 任务创建、轮询、结果展示；覆盖 Token 过期自动刷新、AI 服务不可用、
参数错误、无权限 403、任务失败重试等异常场景；
在 Chrome 和 Edge 检查布局与功能；
编写可重复执行的接口冒烟脚本；修复所有发现缺陷并补关键测试。
验证：验收单全部通过，后端 mvn test 和前端 pnpm build 均通过。
完成后提交缺陷清单和修复说明，不要进入 B10。
```

### B10：文档与交付

**目标**

完成 README、接口文档、数据库设计、部署教程、Docker Compose、答辩/演示材料。

**主要产出**

- README：环境要求、目录说明、三端启动方式、默认账号。
- `docs/api/`：核心接口文档含请求/响应示例。
- `docs/database/`：与 Flyway 一致的数据库设计说明。
- `docs/deploy/`：全新环境部署教程。
- `docker/docker-compose.yml`：mysql、redis、backend、ai-service、frontend 一键编排。
- `.env.example`、启动脚本、架构图、演示/答辩材料大纲。

**验收标准**

- 文档与代码一致，按 README 能从零启动三端。
- Docker Compose 配置可解析；若本机有 Docker 则实际启动验证。
- 交付物清单完整，可直接用于演示和论文支撑。

**B10 提示词**

```text
请完成 B10：文档与交付。
依据项目书第 12.7、15、17 章，补齐：
README（环境、目录、启动、默认账号）；
docs/api 接口文档、docs/database 数据库设计、docs/deploy 部署教程；
docker/docker-compose.yml 编排 mysql/redis/backend/ai-service/frontend；
各服务 .env.example 和启动脚本；架构图与演示/答辩材料大纲。
验证：文档中的命令均能在本机执行；Docker Compose 配置通过 YAML 校验，
若本机有 Docker 则实际启动验证；确认数据库表与 Flyway 一致。
完成后提交最终交付清单和验收报告。
```

## 5. 执行顺序说明

- B1-B3 与 B4 可视为两条主线：后端基础在前，AI 服务独立推进，到 B5 汇合联调。
- B6-B8 依赖后端接口冻结，因此在 B5 之后再开发前端页面，减少返工。
- B9 是全量回归，B10 是交付收尾。
- 每个批次以“验收标准全部通过”为完成条件；若环境缺少 MySQL/Redis/Python/Docker，先补齐或申请审批，不跳过验证。
